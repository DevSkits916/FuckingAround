import clean from './clean.json';
import terminalGreen from './terminal-green.json';
import cardLeft from './card-left.json';
import centeredMinimal from './centered-minimal.json';
import leftRail from './left-rail.json';
import badgeRow from './badge-row.json';
import { PresetMeta } from '../types';

export const PRESETS: PresetMeta[] = [
  clean as PresetMeta,
  terminalGreen as PresetMeta,
  cardLeft as PresetMeta,
  centeredMinimal as PresetMeta,
  leftRail as PresetMeta,
  badgeRow as PresetMeta,
];
